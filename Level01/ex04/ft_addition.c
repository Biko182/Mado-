#include <stdio.h>

int ft_addition(int a, int b)
{  
    int sum;
    sum = a + b ; 

    printf ("%d+%d=%d", a, b, sum);  

    return 0;
}

int main(void)
{
    ft_addition(int a, int b);

    return 0;
}